// admin_modules/menu.js — لوحة تحكم الامبراطور الرئيسية + رسالة منح صلاحية الأدمن

const { sendMessage } = require('../utils');

// ═════════════════════════════════════════════════════════════════════
//   القائمة الرئيسية (لوحة تحكم الامبراطور)
// ═════════════════════════════════════════════════════════════════════

async function handleAdminMenu(api, event) {
  const msg =
    `╗═════━━━❖━━━═════╔\n` +
    `            لوحة تحكم الامبراطور       \n` +
    `╝═════━━━❖━━━═════╚\n` +
    `❖ بيانات \n` +
    `❖ معلومات \n` +
    `❖ بانكاي\n` +
    `❖ بانكاي مؤبد\n` +
    `❖ حذف\n` +
    `❖ الحظر\n` +
    `❖ اشعار \n` +
    `❖ اضافة مهام \n` +
    `❖ تجاهل / فك التجاهل \n` +
    `❖ ازالة الانذارات \n` +
    `❖ تقمص / الغاء التقمص \n` +
    `❖  رتب الادارة \n` +
    `❖ صمت هنا\n` +
    `❖ صمت الكل\n` +
    `❖ فك الصمت هنا\n` +
    `❖ فك الصمت الكل\n` +
    `❖ اعدادات كوينز التفاعل\n` +
    `❖ تفاعلات\n` +
    `❖ مفاتيح رسم\n` +
    `━━━━━━━━━━━━━━━━━━\n` +
    `❖ المشرفون\n` +
    `❖ ريست \n` +
    `❖ ملفات\n` +
    `❖ ايقاف البوت / تشغيل البوت\n` +
    `❖ تأخير \n` +
    `❖ تعديل\n` +
    `❖ تعطيل / تشغيل \n` +
    `❖ مسح\n` +
    `❖ جاسوس\n` +
    `❖ ضبط الاوامر \n` +
    `❖ البوتات \n` +
    `❖ تبديل \n` +
    `❖ اعادة ضبط \n` +
    `❖ الحماية \n` +
    `❖ قاعدة البيانات \n` +
    `❖ طلبات المراسلة \n` +
    `❖ القروبات\n` +
    `❖ قروبات البوت \n` +
    `❖ اضافة \n` +
    `❖ ايدي / ايدي القروب\n` +
    `━━━━━━━━━━━━━━━━\n` +
    `❖ اعدادات قول \n` +
    `❖ منشورات\n` +
    `❖ الوكلاء / ذاكرة`;
  await sendMessage(api, msg, event.threadID);
}

// ═════════════════════════════════════════════════════════════════════
//   منح صلاحيات الأدمن
// ═════════════════════════════════════════════════════════════════════

async function handleAdminGranted(api, event) {
  try {
    await sendMessage(api,
      `╗═════━━━━━═════╔\n ┇            𝑨𝑫𝑴𝑰𝑵 ☑            ┇  \n╝═════━━━━━═════╚`,
      event.threadID);
  } catch (e) {}
}

module.exports = {
  handleAdminMenu,
  handleAdminGranted
};
